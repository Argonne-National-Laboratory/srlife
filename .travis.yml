language: c++
before_install:
      - sudo apt-get install -y build-essential cmake libblas-dev liblapack-dev python3-dev
install:
      - pip install -r requirements.txt
script:
      - nosetests
      - pylint srlife
